apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: docker-registry
  namespace: config-sync-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: volcengine-secret-store
    kind: ClusterSecretStore
  target:
    name: docker-registry
    template:
      metadata:
        labels:
          config-sync: docker-registry
        annotations:
          config.zyql.com/synced-from: config-sync-system
          config.zyql.com/synced-by: docker-registry

      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: |
          {
            "auths": {
              "{{ .registry }}": {
                "username": "{{ .username }}",
                "password": "{{ .password }}",
                "auth": "{{ printf "%s:%s" .username .password | b64enc }}"
              }
            }
          }
  data:
    - secretKey: username
      remoteRef:
        key: /zyql/infra/docker/registry/secret
        property: username
    - secretKey: password
      remoteRef:
        key: /zyql/infra/docker/registry/secret
        property: password
    - secretKey: registry
      remoteRef:
        key: /zyql/infra/docker/registry/secret
        property: registry
